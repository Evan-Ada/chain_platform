# chain_uniapp_mobile

UniApp + Vue 3：App 与微信小程序共用工程（基座占位）。

## 后续计划

- HBuilderX / CLI 创建 UniApp 模板
- 条件编译区分 App 与 `mp-weixin`
- 对接 `chain_fastapi_server` API

## 本地开发

脚手架就绪后使用 HBuilderX 或 `@dcloudio/uni-cli` 运行到对应平台。
