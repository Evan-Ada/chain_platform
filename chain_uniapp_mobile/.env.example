# Chain UniApp Mobile environment example
# 复制为 .env.development / .env.production 后按需修改
#
# 注意：unibest 模板实际加载位于 ./env/ 子目录下的 .env / .env.development / .env.production；
# 本文件仅作为跨子 Agent 接口约定的占位与说明。真实生效请参考 ./env/ 目录。

VITE_API_URL=http://localhost:8000
VITE_APP_NAME=chain App
